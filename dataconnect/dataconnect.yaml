specVersion: "v1beta"
serviceId: "mrdrop"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mrdrop-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
